import React from 'react';
import { FilterButton } from './RecipeList.style';

const RecipeList = function RecipeList() {
  return <FilterButton>generation kill</FilterButton>;
};

export default RecipeList;
